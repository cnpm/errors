export enum ErrorEnum {
  Info = 'info',
  Warning = 'warning',
  Error = 'error',
}

export enum ErrorPrefix {
  info = 'I',
  warning = 'W',
  error = 'E',
}

export enum LoglevelDigit {
  info = '3',
  warning = '2',
  error = '1',
}

export const PLACEHOLDER = "'{%s}'"; // %s 使用索引替换

export const TESTER = 'tester.js';
export const FIXER = 'fixer.js';
export const CONFIG = 'config.js';
